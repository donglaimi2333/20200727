
spring:
   datasource:
     username: root
     password: root
     url: jdbc:mysql://localhost:3306/document?characterEncoding=utf8&useUnicode=true&useSSL=false&serverTimezone=UTC
     type: com.alibaba.druid.pool.DruidDataSource
     driver-class-name: com.mysql.cj.jdbc.Driver

server:
  port: 8080

mybatis:
  mapper-locations: classpath:mapper/*.xml

logging:
  level:
    com.example.demo.dao: debug
